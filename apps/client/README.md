# @nasa-fuel/client

React + Vite frontend for the NASA Fuel Calculator.

Setup, configuration, and usage instructions are in the [root README](../../README.md#frontend).

```bash
# From the repo root
npm run dev          # server + client
npm run dev:client   # client only
```

Environment variable: `VITE_WS_URL` (see `.env.example`).
