---
description: Vitest testing conventions
globs: "**/*.{test,spec}.{ts,tsx}"
alwaysApply: false
---

# Vitest Testing

Acceptance scenario values → [AGENTS.md](../../AGENTS.md).

- Primary target: `packages/fuel-core`
- Co-locate tests with source (`calculate.test.ts`)
- Test pure functions directly — no WebSocket or DOM in fuel-core
- Descriptive `it` blocks; focus on recursive logic + acceptance scenarios
